

This is a simple project meant to demo features for the peercompute library. 

Terrain generation is performed in peercompute computemanager worker threads and stored in indexed db by the statemanager. 