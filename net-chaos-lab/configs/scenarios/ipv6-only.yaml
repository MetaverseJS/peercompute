name: ipv6-only-stability
settle_seconds: 10
stages:
  - at_seconds: 0
    type: ip_mode
    mode: ipv6-only
  - at_seconds: 20
    type: bandwidth_shift
    link: lan_a:core
    bw_mbit: 10
    delay_ms: 25
    loss_pct: 0.5
